# 2026-08-24

- 审计 src/index.mjs（未改代码）。发现 2 个真实 bug：① attach 模式卸载后 pending rAF 重试仍会写调用方 scrollTop（L458-461 无 mounted 守卫）；② wheel cancel 处理器中 anchorAt 无空数据守卫（L694-707），attach 模式下可产生 {key: undefined, viewportOffset: NaN} 的坏锚。
- 另有 7 处易错点：sizes/typeByKey/buckets 对已删 key 永不清理（key 含 index 时雪崩）；'__top'/'__bottom' 与业务 key 冲突且无重复 key 检测；渲染期 ref 副作用（typeByKey.set / prevData / apiRef.current / RO 实例化）；noteMeasured 在 item 换 type 时旧桶不回退（桶平均漂移）；apiRef 方法每次渲染换 identity；data 原地 push 不换 identity 不触发重算（隐式契约未写入 README）；双实例共享 scrollElement 时 overflowAnchor 恢复互相覆盖。
- 简化建议 6 条：useLiveRef 消 8 处镜像 ref；key 按 data identity 只派生一次（现为每项每轮调用两次）；GLOBAL_BUCKET/PRIOR_SAMPLES/三个 epsilon 常量提升并命名；rAF 重试加 in-flight + mounted 标志；typeByKey.set 移入 recompute；writeSpacers 空 window 提前 return。
